# Perception (SAM 3) dependencies.
# Python 3.12 venv required — PyTorch CUDA wheels don't support the system Python 3.14.
#
# torch and torchvision are installed separately from the PyTorch CUDA index (see README),
# not from this file, because the right wheel depends on the local CUDA driver version.

transformers>=4.56
accelerate
huggingface_hub[cli]
numpy
pillow
opencv-python
imageio
imageio-ffmpeg
gradio
requests
ultralytics  # YOLOE backend (yoloe_runner.py) — downloads its own weights + a ~570MB
             # MobileCLIP text encoder on first use, separate from the facebook/sam3 cache.
